//#include "lib/Graph.h"
#include <stdio.h>
#include <stdlib.h>
#include "lib/Queue.h"
#include "lib/Graph.h"


int main(int argc, char *argv[]){
    
    int V = 5;
    // Graph *g = from_file("grafo.g");
    
    // Graph* graph = create_graph(V);
    // add_edge(graph, 0, 1);
    // add_edge(graph, 0, 4);
    // add_edge(graph, 1, 2);
    // add_edge(graph, 1, 3);
    // add_edge(graph, 1, 4);
    // add_edge(graph, 2, 3);
    // add_edge(graph, 3, 4);
    
    // print_graph(graph);
    mat_from_file("grafo.g");
 
    return 0;
}
